# 车辆适配器思路

车辆作者通常需要完成三步：

1. 读取自己的车辆状态，转换为公开 `Telemetry`。
2. 将允许的标准命令映射到自己的车辆接口。
3. 在执行前检查车速、供电、门状态和本地驾驶优先级。

示例：

```text
door.front.open
  → 检查车速接近 0
  → 检查该车型支持前门远控
  → 调用车辆作者自己的开门接口
  → 返回 success 或 rejected
```

公开 SDK 不规定具体变量名称。开发者不应把其他车辆的内部实现复制到自己的适配器中。
